package com.company;

public class Main {

    public static void main(String[] args) {

        /* Operadores lógicos
        * && == E
        * || == OU
        * ! == NOT
        *
        * > == Maior que
        * < == Menor que
        * == == igual
        * >= == maior ou igual que
        * <= == Menor ou igual que
        * != == diferente
        */

        /* Estruturas de condição/controle
        * if( "condição" ) {}
        * else if( "condição" ) {}
        * else {}
        */

        /* Operadores cumulativos
        * -= == a = a - b
        * += == a = a + b
        * += == a = a * b
        * /= == a = a / b
        * %= == a = a % b  -> resto da divisão
        */

        /* Switch Case
        * switch( variável/expressão ) {
        *   case "valor":
        *       comando
        *       break -> para não seguir os próximos cases
        *   case "outro valor":
        *       comando
        *       break
        *   default:
        *       comando padrão se nenhuma das outras for atendida
        *       break
        * }
        */

        /* Expressão ternária
        *
        * ( condição ) ? valor_se_verdadeiro : valor_se_falso
        *
        * Ex.: double preco = 34.5;
        *      ( preco < 30 ) ? System.out.println("Está num preço bom!") : System.out.println("Está muito caro!")
        *
        */
    }
}
